<%= form_for(@team) do |t| %>

  <div class="field"> <%= t.label :name %><br>
  <%= t.text_field :name %></div>

  <div class="field"><%= t.label :league %><br>
  <%= t.text_field :league %></div>

  <br>

  <div class="actions"><%= t.submit 'Submit', :class => 'btn btn-primary' %></div>

<% end %>
